Metadata-Version: 2.4
Name: stock-agent-lab
Version: 0.1.0
Summary: A lightweight multi-agent A-share stock analysis lab.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: akshare>=1.17.86
Requires-Dist: langgraph>=0.4.8
Requires-Dist: openai<2.0.0,>=1.0.0
Requires-Dist: pandas>=2.0.0
Requires-Dist: python-dotenv>=1.0.0
Requires-Dist: rich>=13.0.0
Requires-Dist: typer>=0.12.0
Provides-Extra: dev
Requires-Dist: pytest>=8.0.0; extra == "dev"

# stock-agent-lab

一个用于练手的多 Agent A 股分析项目。

当前版本重点是：

- 用 `LangGraph` 编排多 Agent 工作流
- 用 `AkShare` 获取 A 股历史行情和新闻
- 用 `DeepSeek` 或其他 OpenAI-compatible 模型生成分析
- 输出 Markdown 和 JSON 报告

项目目标不是完全复刻 `TradingAgents-CN`，而是参考它的多 Agent 思路，逐步做一个更轻量、更容易理解、便于自己继续演进的版本。

## 当前工作流

当前 graph 已经包含两类回路：

- 工具回路
  - `Technical Analyst -> tools_technical -> Technical Analyst`
  - `News Analyst -> tools_news -> News Analyst`
- 多空辩论回路
  - `Bull Researcher <-> Bear Researcher`

之后进入：

- `Research Manager`
- `Trader`
- `Risk Manager`

如果启用 `--include-node-trace`，会把这次运行的 `node_trace` 一起写进报告。

## 项目结构

```text
stock-agent-lab/
├─ docs/                     项目状态与后续优化文档
├─ src/stock_agent_lab/
│  ├─ agents/                多 Agent 角色
│  ├─ data/                  AkShare 数据获取与技术指标
│  ├─ graph/                 LangGraph 工作流编排
│  ├─ llm/                   OpenAI-compatible / DeepSeek / Mock 客户端
│  ├─ reports/               Markdown / JSON 报告输出
│  ├─ cli.py                 CLI 入口
│  └─ pipeline.py            graph -> report 稳定入口
├─ tests/                    pytest 测试
├─ .env.example              环境变量模板
├─ pyproject.toml            项目依赖配置
└─ README.md
```

## 环境准备

```powershell
python -m venv .venv
.\.venv\Scripts\pip install --upgrade pip
.\.venv\Scripts\pip install -e ".[dev]"
```

## 环境变量

推荐使用 DeepSeek。

你可以先复制一份环境变量模板：

```powershell
Copy-Item .env.example .env
```

然后把真实 key 写进你自己的 `.env`，不要写进 `.env.example`。

### DeepSeek

```env
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
```

### OpenAI-compatible 兼容配置

如果你以后切换其他兼容后端，也可以使用：

```env
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
```

`OpenAICompatibleClient` 当前支持：

- 优先读取 `OPENAI_*`
- 如果没有，再读取 `DEEPSEEK_*`

## 运行方式

### 1. 本地 mock 演示

不依赖真实模型，适合先看完整流程：

```powershell
.\.venv\Scripts\stock-agent analyze --symbol 600519 --days 30 --mock-llm
```

### 2. 真实 AkShare + 真实 DeepSeek

```powershell
.\.venv\Scripts\stock-agent analyze --symbol 600519 --days 5
```

### 3. 输出执行轨迹

```powershell
.\.venv\Scripts\stock-agent analyze \
  --symbol 600519 \
  --days 5 \
  --include-node-trace \
  --output-dir outputs\deepseek_real_run
```

## 输出结果

默认输出目录：

```text
outputs/<symbol>_<date>/
```

主要文件：

- `report.md`
- `result.json`

如果开启 `--include-node-trace`：

- `report.md` 会多一个“执行轨迹”小节
- `result.json` 会包含 `node_trace`

## 数据层说明

当前 A 股数据获取基于 `AkShare`，并做了轻量增强：

- 历史行情优先走 `stock_zh_a_hist`
- 失败后回退到 `stock_zh_a_daily`
- 对东方财富请求补浏览器 headers
- 代理错误时做一次无代理重试

这让当前项目在更多网络环境下更容易拿到真实行情。

## 测试

运行全量测试：

```powershell
.\.venv\Scripts\python -m pytest
```

单独运行数据层测试：

```powershell
.\.venv\Scripts\python -m pytest tests\test_data_provider.py
```

单独运行 LLM 配置测试：

```powershell
.\.venv\Scripts\python -m pytest tests\test_llm_client.py
```

## 项目现状文档

后续请优先看这份文档了解项目当前状态和缺口：

- [docs/project_status.md](d:/agent_project/stock-agent-lab/docs/project_status.md)

这里持续维护：

- 当前已实现能力
- 外部链路现状
- 与原项目相比还缺什么
- 后续优化优先级

## 免责声明

本项目仅用于学习多 Agent 编排、数据分析流程和工程实现，不构成任何投资建议或交易依据。
